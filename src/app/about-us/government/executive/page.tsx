import {MenuItems} from "@/components/menu-items";
import ExecutiveData from "@/components/ExecutiveData";


const Executive = () => {
    return <MenuItems data={ExecutiveData}/>;
};

export default Executive;
